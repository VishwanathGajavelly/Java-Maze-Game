import java.awt.*; //To show the GUI to the User
import javax.swing.*; //To build the window to add button, image into the GUI

public class Image extends JPanel 
{
	public int w=Maze.width;
	public int h=Maze.height;
	public Image() // Constructor of Image Class
	{
		this.setBackground(Color.BLACK); //set the background of the panel to black
	}
	
}
